document.getElementById('viewing-form').addEventListener('submit', function(event) {
  event.preventDefault();
  
  // Code to handle form submission (e.g., sending email or storing request)
  alert("Viewing request submitted!");
});